    <!-- Team Member 1 -->
    <div class="col-xl-3 col-md-6 mb-4">
      <div class="card border-0 shadow">
        <img src={{this.image_Path}} class="card-img-top" alt="...">
        <div class="card-body text-center">
          <h5 class="card-title mb-0">{{this.nombre}}</h5>
          <div class="card-text text-black-50">{{this.descripcion}}</div>
          <button type="button" id="{{this.nombre}}_button"class="btn btn-success" onclick="buy({{toString this.nombre}})">Comprar</button>
        </div>
      </div>
    </div>